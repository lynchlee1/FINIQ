# FINIQ 공통 흐름 익히기

이 튜토리얼에서는 FINIQ를 처음 쓰는 사람이 작업공간을 고르고 비동기 작업을 확인하는 과정을 익힌다. 화면에 보이는 결과와 파일에 저장한 결과를 구분하는 방법도 다룬다. 기능별 사양과 작업 안내는 중앙 [README.md](../../README.md)에서 찾는다.

## 1. 작업공간 고르기

화면에서 `작업공간 디렉토리`를 고른다. 별도 저장 경로를 켜지 않으면 공시분석은 이 폴더 아래에서 단계별 입력과 결과를 찾는다.

공시분석 결과는 01~07단계와 09단계 폴더로 나뉜다. 08단계는 07단계 결과를 읽어 보여 주므로 08단계 폴더가 없어도 정상이다. 정확한 폴더 이름은 [공시분석 공통 사양](../../disclosures/common/reference.md)에서 확인한다.

Ontology 주가 자료도 작업공간 아래 표준 경로에서 찾는다. 정확한 위치는 [Ontology 공통 사양](../../ontology/common/reference.md)에서 확인한다.

## 2. 비동기 작업 확인하기

중앙 문서 지도에서 사용할 기능 사양을 열어 입력을 확인한 뒤 작업 하나를 실행한다. 실행 중 다른 화면으로 옮겼다가 돌아와도 화면은 저장한 작업 ID로 같은 작업을 찾아 진행 상태를 이어서 보여 준다.

상태 조회가 `404`를 반환하면 화면은 저장한 작업 ID를 지운다. 결과 본문, 실패 이유, 완료 결과를 읽지 못하면 성공으로 처리하지 않는다. 서버가 실패 이유를 보내지 않으면 화면에 `Job failed`를 표시한다.

공시분석 상태값은 공시분석 [공시분석 공통 사양](../../disclosures/common/reference.md), Ontology 상태값은 Ontology [Ontology 공통 사양](../../ontology/common/reference.md)에서 확인한다.

## 3. 화면 결과와 저장 결과 구분하기

진행 내역과 오류 예시는 화면 크기에 맞춰 일부만 보일 수 있다. 긴 파일 경로는 마지막 네 부분만 남기고 앞을 `.../`로 줄인다. 화면 표시를 줄여도 저장 결과와 전체 검사 대상은 바뀌지 않는다.

숫자 `0`은 빈 값이 아니다. 설정을 저장하지 못해도 현재 입력값은 화면에서 지우지 않는다. 화면에 값이 남아 있다는 사실만으로 저장에 성공했다고 판단하면 안 된다. 공시분석 표시 기준은 공시분석 [공시분석 공통 사양](../../disclosures/common/reference.md), Ontology 표시 기준은 Ontology [Ontology 공통 사양](../../ontology/common/reference.md)에서 확인한다.

## 다음 작업

작업 중 판단이 필요한 상태를 만나면 알맞은 작업 안내를 따른다.

- [01단계 결과 다시 받기](../../disclosures/01-download/how-to-guides.md)
- [기존 HTML을 확인하고 기준 해시 만들기](../../disclosures/html-download/how-to-guides.md)
- [처음 보는 목차 검토하기](../../disclosures/06-html-section-split/how-to-guides.md)

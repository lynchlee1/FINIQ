# 연도별 SQLite 변환

## 기능 범위

- HTML로 저장한 KIND 조건검색 결과를 연도별 SQLite 조각으로 바꾼다.

## 자료 흐름

- 입력 경로는 `<data_root>/01-list`, 저장 경로는 `<data_root>/02-table`이며, 저장 형식은 아래와 같다.
```text
<data_root>/
├── 01-list/
│   └── <YYYYMMDD>_<YYYYMMDD>/
│       └── *_post_page_*.body
└── 02-table/
    ├── <YYYY>.sqlite
    └── sqlite_manifest.json
```

## 처리 계약

### 정상 동작

#### HTML 단일 출처 사용

다른 표를 읽지 않도록 구조를 하나로 고정한다.
- `summary`에 `회사명`과 `공시제목`이 모두 있는 표가 하나만 있을 때 해당 표를 읽는다.
- 공시 행은 해당 표 바로 아래에 하나만 있는 `tbody > tr`에서 읽는다.

#### 연도별 SQLite 만들기

연도별 SQLite 조각을 만든다.
- 공시일에서 연도를 읽어 `<YYYY>.sqlite` 조각을 만든다.

#### 공시 행 중복 정리

같은 공시를 한 번만 저장하면서 제거된 원본 행도 집계한다.
- 모든 원본 `tbody > tr`을 순서대로 확인한다.
- `acpt_no`가 같은 행은 같은 공시로 보고 먼저 읽은 행만 SQLite에 저장한다.
- 나중에 읽은 같은 `acpt_no` 행은 버리지 않고 중복 행 수에 포함한다.

#### 회사 표시 정보 추출

회사 칸에 이미지가 있는지 확인해 시장과 badge를 정한다.
- 이미지가 있으면 첫 번째 이미지에서 읽은 `alt`를 시장으로, 나머지 이미지에서 읽은 `alt`를 badge로 저장한다.
- 회사 칸에 이미지가 없으면 시장은 `null`, badge는 빈 목록으로 저장한다.

#### SQLite manifest 만들기

완성한 SQLite 조각마다 출처와 검증 근거를 기록한다.
- SQLite 조각을 모두 만들면 `sqlite_manifest.json`에 원본 경로, 테이블 이름, 전체 원본·중복·저장 행 수와 연도별 SQLite 경로·저장 행 수를 기록한다.
- 페이지별 원본 파일, 페이지 번호와 원본·중복·저장 행 수도 기록한다.

#### SQLite 무결성 검사

SQLite 조각이 제대로 만들어졌는지 검사한다.
- 페이지마다 `원본 행 수 = 저장 행 수 + 중복 행 수`인지 확인한다.
- 전체 건수도 `원본 행 수 = 실제 SQLite 행 수 + 중복 행 수`인지 확인한다.
- 연도별 SQLite 실제 행 수가 `sqlite_manifest.json` 저장 행 수와 같은지 확인한다.
- 연도별 저장 행 수를 합친 값이 `sqlite_manifest.json`에 기록한 전체 저장 행 수와 같은지 확인한다.

### 중단 조건

#### 원본 구조 오류가 나면 실패 처리

원본 페이지나 결과표 범위를 확정할 수 없으면 변환을 중단한다.
- `<data_root>/01-list`에 `*_post_page_*.body`가 없거나 파일을 읽을 수 없는 경우 실패 처리한다.
- `kind_workflow.input.json`이 있는 연도별 입력 폴더에 공시 결과 페이지가 하나도 없거나 같은 페이지 번호를 가진 원본 파일이 둘 이상이면 실패 처리한다.
- 공시 결과 파일명이 `_post_page_<숫자>.body`로 끝나지 않아 페이지 번호를 확정할 수 없으면 변환과 원본 직접 조회를 실패 처리한다.
- `summary`에 `회사명`과 `공시제목`이 모두 있는 표가 없거나 둘 이상이면 실패 처리한다.
- 결과표 바로 아래에 `tbody`가 없거나 둘 이상이면 실패 처리한다.
- 본문 행 바로 아래에 있는 `td`가 5개보다 적으면 실패 처리한다.

#### 페이지를 읽지 못하면 변환 중단

손상된 입력을 재읽기나 복구 record로 숨기지 않는다.
- 원본 `.body`를 한 번 읽어 결과표를 확정하지 못하면 즉시 실패 처리한다. 공시 행에 필요한 식별값이 잘못된 경우는 행 변환 규칙에서 처리한다.
- 페이지별 pagination 값이 서로 다르거나 원본 페이지가 연속적이지 않으면 다수결이나 복구 overlay를 사용하지 않고 실패 처리한다.
- workflow metadata에 page size·요청 간 대기 시간·timeout을 모두 명시해야 한다.

#### 표를 만들 입력에 오류가 나면 변환 중단

원본과 출력 위치를 추측하지 않는다.
- KIND 원본 폴더는 `root_directory`, 출력 위치는 `output_path`에 명시해야 한다.
- `classification_path`, 구형 JSON classification과 이름순 파일 탐색은 지원하지 않는다.

#### 공시 행 필수값 오류가 나면 실패 처리

공시 행에서 SQLite에 저장할 식별값과 표시값을 확정하지 못하면 변환을 중단한다.
- 회사 칸에 유일한 `a#companysum`이 없거나 회사 ID·회사명 `title`을 읽지 못하면 실패 처리한다.
- 제목 칸에 유일한 공시 링크가 없거나 표시 제목·`acpt_no`를 읽지 못하면 실패 처리한다.
- 회사 칸에 이미지가 있으면 모든 이미지에 비어 있지 않은 `alt`가 있어야 하며, 하나라도 비어 있으면 실패 처리한다.
- 공시 링크 식별값 중 `acpt_no`는 필수값이다.

#### SQLite를 만들지 못하면 변환 중단

공시 연도나 검색 표를 확정하지 못한 조각을 저장하지 않는다.
- 공시일이 네 자리 연도로 시작하지 않거나 SQLite FTS5 표를 만들 수 없으면 실패 처리한다.

#### 행 수 검증을 통과하지 못하면 변환 중단

원본 행이 저장 또는 중복 집계에서 빠진 결과를 사용하지 않는다.
- manifest에서 페이지별 `원본 행 수 = 저장 행 수 + 중복 행 수`가 성립하지 않으면 실패 처리한다.
- manifest에서 `원본 행 수 = 실제 SQLite 행 수 + 중복 행 수`가 성립하지 않으면 실패 처리한다.
- manifest에 기록한 연도별 저장 행 수나 전체 저장 행 수가 실제 SQLite 행 수와 다르면 실패 처리한다.

## 화면과 서비스 계약

### 정상 동작

#### 무결성 오류 표시 범위 제한

무결성 검사 결과는 그대로 두고 화면에 넘길 값만 줄인다.
- 화면에 보여 주는 범위는 [공통 화면 사양](../../common/common-ui/reference.md)를 따른다.

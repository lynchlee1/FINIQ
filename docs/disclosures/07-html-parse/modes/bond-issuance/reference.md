# 사채발행 변환 예외 사양

## 처리 계약

### 중단 조건

#### 사채 필수 표가 누락되면 중단

사채 조건이나 투자자를 확정하지 못한 원문에서 일부 값만 만들지 않는다.
- `사채의 종류`, `사채의 권면`, `자금조달의 목적`을 모두 가진 표가 없으면 즉시 실패 처리한다.
- 첫 줄에서 서로 다른 열에 `발행 대상자명`과 `발행권면`이 있는 표가 없으면 즉시 실패 처리한다.

그 밖의 중단 규칙은 [공시원문 변환 예외 사양](../../common/reference.md)을 따른다.

# Asset Transaction Parse Features

## Purpose

`asset_transaction` mode로 유무형자산거래 공시를 읽는다.

## Features

### Select the Asset Transaction Mode

#### Behavior

`asset_transaction` mode에 공통 원본 표 변환을 적용한다.

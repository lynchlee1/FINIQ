# HTML Change Log Reference

## Paths

- `<data_root>/07-converted/<mode>/parsed-<mode>.json`을 입력으로 받아 정정내역을 조회하며 파일은 저장하지 않는다.

### `<data_root>/07-converted/<mode>/parsed-<mode>.json`

#### I/O Structure

- 문서 순서와 correction family, mode별 비교 항목을 담은 입력 파일이다.

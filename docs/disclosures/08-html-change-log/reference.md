# 정정내역 비교 Reference

## 경로와 형식

- 입력 경로는 `<data_root>/07-converted/<mode>/parsed-<mode>.json`이다.
- 08단계는 조회 화면이며 별도의 `08-*` 폴더나 결과 JSON을 만들지 않는다.

```text
<data_root>/
└── 07-converted/
    └── <mode>/
        └── parsed-<mode>.json
```

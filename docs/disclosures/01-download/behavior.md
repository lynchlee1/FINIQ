# KIND 조건검색 결과 저장 정상 동작

기본 자료 흐름과 정상 실행 계약을 설명한다.

## 자료 흐름

- 저장 경로는 `<data_root>/01-list`이며 저장 형식은 아래와 같다.
- 하위 폴더 이름에는 연도별 시작일과 종료일을 YYYYMMDD 형식으로 기록한다.
```text
<data_root>/
└── 01-list/
    └── <YYYYMMDD>_<YYYYMMDD>/
        ├── *_post_page_*.body
        ├── kind_workflow.input.json
        └── kind_workflow.checkpoint.json
```

## 처리 계약

### 정상 동작

#### 기존 데이터 무결성 검사

자료를 최신으로 유지하려고 연도별 폴더에서 무결성을 검사한다.
- 다운로드 실행 전에는 항상 무결성 검사를 실행하며 무결성이 확인된 경우에만 데이터를 다운로드한다.
- 다음 조건으로 기존 결과가 유효한지 판정한다.
  1. `kind_workflow.input.json`의 조건으로 연도별 재검색한 전체 공시 수가 저장값과 같음
  2. 페이지 번호가 중복 없이 1부터 연속함
  3. 본문 간 전체 페이지 수와 건수가 일치함

#### metadata 만들기

자료 무결성을 검사하고 복원할 수 있도록 metadata를 만든다.
- 정상 다운로드는 연도별 폴더마다 `kind_workflow.input.json`과 `kind_workflow.checkpoint.json`을 만든다.
  - `kind_workflow.input.json`은 정상 다운로드가 끝났을 때만 만든다.
  - `kind_workflow.input.json`에는 버전 기록인 `format=finiq_kind_workflow_input_v1`를 필수로 표기한다.
- 두 파일은 다운로드할 공시나 본문을 바꾸지 않고 완료 결과의 조건과 검증 상태를 증명한다.

#### 다운로드 목록 일관성 확인

한 검색기간의 결과가 같은 시점의 KIND 목록으로 구성됐는지 게시 전에 확인한다.
- 한 기간의 모든 페이지를 받은 직후 같은 검색 조건으로 첫 페이지를 다시 요청한다.
- 다시 받은 페이지 정보와 공시 행이 첫 다운로드와 같을 때만 새 임시 결과를 게시한다.

## 화면과 서비스 계약

### 정상 동작

#### 다운로드 실행 기본값

생략 가능한 요청값을 정상 계약에 정의된 값으로 채운다.
- 회사명은 `""`, 제출인은 `""`, 시장은 `전체`, 증권 종류는 `전체`로 채운다.
- 시작 페이지는 1, 페이지 크기는 100, 요청 간 대기는 1초, 제한 시간은 20초다.
- 병렬 처리 방법을 고르지 않으면 연도 단위로 나누어 실행한다.
- 진행 내역 보관 수를 정하지 않으면 20줄을 사용하며 최대 500줄까지 보관한다.

#### 누락 페이지 표시 범위 제한

전체 누락 결과를 바꾸지 않고 화면에 전달할 범위만 제한한다.
- 화면에 보여 주는 범위는 [공통 동작 문서](../common/behavior.md)를 따른다.

#### 진행 내역 전달·보관 범위 제한

다운로드 결과와 관계없는 진행 내역의 화면 전달 범위를 정한다.
- 직접 실행할 때는 진행 내역을 실행 중에만 전달한다.
- 백그라운드 작업은 정해진 줄 수만큼 최근 내역을 보관한다.

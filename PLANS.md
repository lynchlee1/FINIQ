# Completed Changes Requiring Follow-up

## 2026-08-12 — 공시 로컬 검사 중복 I/O와 본문 누적 축소

### 목적

- 04·05 기존 HTML 검사에서 구조 판별과 SHA-256 계산 때문에 같은 파일을 두 번 읽던 비용을 없앤다.
- 02 변환·기존 데이터 검토에서 같은 입력 트리를 반복 탐색하고 페이지 번호를 다시 계산하던 비용을 없앤다.
- 06 저장 결과 검사에서 모든 예상 HTML 본문을 비교 종료까지 보관하던 메모리 증가를 없앤다.

### 구현 요약

- 기존 HTML을 한 번 순차 읽으면서 구조 식별자와 SHA-256·바이트 수를 함께 계산한다. 다운로드 후 판별과 압축 JSON 저장 후 재검증은 변경하지 않았다.
- 02 실행마다 원본 폴더·BODY 경로·페이지 번호를 담은 인벤토리를 한 번 만들고 검증·파싱·집계가 공유한다. 인벤토리는 실행 사이에 저장하지 않는다.
- 06 검사에서 예상 HTML을 생성 결과가 나오는 즉시 실제 파일과 비교하고 상대 경로·건수만 유지한다.
- 응답 형식, 입력 순서, 중복 제거, 오류 조건과 독립 검증 절차는 유지했다.

### 검증 결과

- 변경 경로 집중 회귀: `374 passed, 166 skipped`
- 전체 MarketDesk 및 KIND 관련 테스트: `669 passed, 166 skipped, 4 failed`
- 기존 실패 4건은 classification의 구형 JSON 기대 2건과 fallback 입력 검증 순서 기대 2건으로 변경 전과 같다.
- 프런트 정적 테스트: `160 passed`
- 전체 프런트 production build: 통과
- 변경 Python 파일 `py_compile` 및 `git diff --check`: 통과

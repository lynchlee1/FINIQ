# Completed Changes Requiring Follow-up

## 2026-08-13: bond issuance external HTML storage migration

- Purpose: align the legacy `database/04-external-html-download/bond_issuance` data with the current year-partitioned external HTML layout.
- Implementation: moved 15,176 HTML files from `kind_html/` into `2008/` through `2026/`, moved `kind_disclosure_html_manifest.json` to the mode root, and removed the empty legacy directory.
- Verification: confirmed a one-to-one match between manifest records and HTML files with no duplicate, missing, extra, invalid-date, or year-mismatched entries; confirmed the current yearly HTML collector finds all 15,176 files; `pytest -q tests/market_desk/test_partition_utility.py` passes (7 tests).
- Unresolved finding: the stage-04 manifest remains in the legacy provenance format without `source_fingerprint`, `source_sha256`, or `source_size_bytes`. The current resume path therefore treats all existing files as hash-unverified. Database-linked regeneration is intentionally deferred.

## 2026-08-13: three-mode external/internal HTML data alignment

- Purpose: align `bond_issuance`, `rights_issuance`, and `shareholder_meeting` data in stages 04 and 05 with the current year-partitioned storage and compressed-record contracts.
- Implementation: moved the flat stage-05 bond and rights HTML collections into manifest-derived `2008/` through `2026/` directories; created one current-format `compressed-external-html.json` at each stage-04 mode root; replaced 19 redundant shareholder meeting year-level legacy compression files with the verified root file; regenerated the shareholder meeting stage-05 manifest with source fingerprint and per-file integrity metadata.
- Verification: stage-04 current compression parsed and reloaded all 107,116 external HTML files; all stage-04 manifest, compressed-record, stage-05 manifest, and HTML receipt-number sets match with no duplicates or year-path mismatches; 29 storage/workspace tests pass. Stage-05 content validation found two preserved corrupt legacy files: empty bond file `2016/20160819000357.html` and NUL-corrupted rights file `2016/20160330002146.html`. Their stage-05 manifests remain in the legacy provenance format until those files are restored and the manifests can be regenerated completely.
- Unresolved finding: the stage-04 manifests for all three modes and the stage-05 manifests for bond and rights do not satisfy the current provenance contract. The two known corrupt stage-05 files also remain unresolved. Database-linked repair and verification are intentionally deferred.

## 2026-08-14: shareholder-meeting entity and relationship extraction

- Purpose: turn shareholder-meeting notices and results into evidence-backed people, organizations, agendas, and phase-aware relationships without treating candidates as current officers or inferring unnamed parties; preserve a durable record of successful and failed extraction approaches for the next session.
- Implementation: kept runtime extraction deterministic and tracked a 34-receipt multi-agent-adjudicated golden corpus; added structured agendas, four election roles, document-local entities and 19 relationship types covering elections, proposals, current affiliations, auditors, electronic voting, terminations and named transactions. Exactly two failure-triggered fallbacks remain: structured agenda schema absent to one direct-cell legacy source, and zero named election matches to one uniquely compatible unnamed election agenda. Alternative text grammars for transfers, auditors, business-purpose mergers, voting managers and voting periods are evaluated as independent candidate sets rather than failure-triggered rescue branches. Parser and graph inputs otherwise use canonical fields and first sources without metadata, date, DOM, identity, evidence or decoder substitution. Same-cell `other_company` line breaks are handled as one explicit multiline grammar, while split role tokens such as `사내\n이사` are rejected. Person identity no longer upgrades a birthless entity by lookup; one shared birth month across compatible election offices is selected before the agenda person is materialized. See `docs/disclosures/07-html-parse/modes/shareholder-meeting/handoff-2026-08-14.md` for the implementation history, failed approaches and restart procedure.
- Verification: the frozen production SHA-256 values are `parser=a5402f8194c82c4c15878660733c8894f5ac3fd5efc1f5ae16a249acd19f3ca3`, `semantics=b1a221ba3e9b1c682930fc0b176b03a551693dcad0703b2eaffd4293ada0775b`, `stakeholders=e32f90d251e702863a66dd9dfa04c886f348f15af50d7c16abcfee7e45793943`, `ontology=c8f9ad46acdc4ff0f5583c65263d0db63b44c47bf0ac159bd5a4e0f0c8b40077`, and `web=08ba1e4c83772a2242560cb518bcecdb35382aeeec9242556e0e4a675c0e1270`. Golden verification passes 36 tests against 34 receipts and 68 byte-fixed full HTML files (1,478,272 bytes). The six shareholder-meeting suites report 501 passed and one skipped; ontology builder reports 22 passed; related KIND/web integration reports 385 passed and 166 skipped. The full repository reports 1,358 passed, 167 skipped and four failures outside this scope: two classification SQLite contract failures and two HTML-download validation-order failures. The final immutable 2026 audit parsed all 4,389 external/internal pairs with zero exceptions or hard invariant violations, producing 29,911 agendas, 9,565 election rows, 10,964 entities, 56,412 relationships and 2,316 business-purpose changes. Snapshot start/end and live-end hashes all matched the freeze. The report is `/private/tmp/finiq-handoff-final4-2026.ZFZ7p4/report.json` (SHA-256 `bf9316dc5a332f63b9a1d005aaba77acd29f7d73f614701e4c44b9c70865649d`). The multiline grammar restored 92 true `serves_at` facts across 78 receipts and removed the one known split-role false positive; identity hardening leaves only one intentional birthless termination subject instead of merging it into a born appointment identity.
- Unresolved finding: the final production snapshot has not been rerun over all 71,965 historical pairs, so earlier full-history totals are superseded and must not be presented as current verification. Free-text extraction remains deliberately high-precision rather than exhaustive; ambiguous career statements, unknown proposal wording and unnamed or implicit transaction counterparties are not inferred. Person resolution remains issuer-scoped, and ShareholderMeeting identity remains disclosure-scoped, so separate NOTICE and RESULT receipts for the same physical meeting are not merged automatically.

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "finiq-graphviewer"
version = "1.2.0"
description = "FINIQ Graph Viewer"
readme = "README.md"
requires-python = ">=3.10"

# 공시 파이프라인 01–07 구현 현황과 다음 단계

> 기준일: 2026-08-12
> 원칙: 결과, 검증 강도, 실패 조건과 API 형식을 바꾸지 않는 개선부터 적용한다.

## 핵심 요약

현재까지는 중복 파일 읽기와 실행 중 계속 커지던 임시 메모리를 줄였다. 처리 결과와 입력 순서, 중복 제거 기준, 저장 후 검증은 그대로다. 다음 핵심 과제는 **02 공시내역 변환의 전체 변환 행 메모리 보관**과 **07 변환의 누적 JSON 재작성**이다.

## 지금까지 구현한 내용

| 단계 | 변경 내용 | 달라진 점 |
| --- | --- | --- |
| 01 공시 다운로드 | 페이지 BODY를 한 번만 읽어 pagination과 공시 행을 각각 검사한다. | 같은 파일을 검사 과정에서 중복으로 읽지 않는다. 기존 오류 문구와 판정 기준은 유지한다. |
| 02 공시내역 변환 | 실행마다 원본 경로를 한 번 열거한 인벤토리를 검증·파싱·집계가 공유한다. 모든 페이지의 원본 파싱 결과는 한꺼번에 모으지 않고 최대 `table_workers × 2`개씩 입력 순서대로 소비한다. | 같은 실행에서 원본 경로와 페이지 번호를 반복 탐색하지 않고, 원본 파싱 결과도 전체 페이지 수만큼 쌓이지 않는다. 인벤토리는 실행 사이에 저장하지 않는다. 접수번호 중복 제거, 연도 분류와 SQLite 저장 순서는 이전과 같다. 다만 변환이 끝난 행은 아직 연도별 목록에 모두 보관한다. |
| 02 기존 데이터 검사 | Next.js 프록시 제한을 10분으로 늘리고, 공시 행 전체 대신 페이지·연도별 건수만 작은 배치로 집계한다. | 실제 31,522페이지·3,150,494행을 318.5초, 최대 RSS 약 635MB로 검사했고 37개 SQLite 파일과 변환 기록이 일치했다. |
| 04·05 HTML 저장 | 기존 HTML의 구조 판별과 SHA-256 계산을 한 번의 순차 읽기로 합쳤다. 진행 내역은 실행 중부터 최근 100줄로 제한하고, 04 압축은 최종 record를 만든 뒤 worker의 중간 tuple 목록을 해제한다. | 기존 파일 검사에서 같은 HTML을 다시 읽지 않는다. 새 다운로드의 저장 후 판별과 압축 JSON 저장 후 재검증은 유지한다. |
| 06 목차 HTML 저장 | 진행 내역을 최근 200줄로 제한하고, 목차 조합을 문서 전체 보관 없이 입력 순서대로 집계한다. 저장 결과 검사도 예상 HTML을 실제 파일과 즉시 비교한다. | 검사 중 예상 HTML 전체가 문서 수에 비례해 쌓이지 않고 상대 경로와 건수만 남는다. |
| 07 HTML 변환 | 결과에 쓰이지 않던 내부 로그 복제를 제거하고, metadata 임시 index를 최종 index에 병합한 항목부터 해제한다. | 변환과 metadata 병합 중의 임시 메모리가 줄었다. 저장되는 결과 형식은 같다. |

## 문서의 의도에 따라 유지한 검증

아래 절차는 계산 낭비처럼 보여도 정확성과 재사용 가능 여부를 보장하기 위해 문서에 명시되어 있으므로 변경하지 않았다.

- 03: 필터 실행 전 실제 SQLite 건수를 검증한다. ([필터 구성](docs/disclosures/03-filter/filtering.md))
- 04: 압축 JSON을 저장한 뒤 다시 읽어 파일 구조와 `records`를 검증한다. ([압축](docs/disclosures/04-external-html/compression.md))
- 05: 저장한 HTML을 다시 판별한다. ([내부 HTML 저장](docs/disclosures/05-internal-html/download.md))
- 06: 하나라도 저장 규칙을 만족하지 않으면 쓰기를 시작하지 않도록 전체 입력을 먼저 확인한다. ([처리 흐름](docs/disclosures/06-section-split/workflow.md))
- 07: `skip_errors=true`일 때 `progress_interval`마다 현재 전체 결과를 중간 저장한다. ([단계 문서](docs/disclosures/07-html-parse/index.md), [설정 기준](docs/disclosures/07-html-parse/reference.md))

## 앞으로 구현할 내용

| 우선순위 | 대상 | 구현 방향 | 먼저 지켜야 할 조건 |
| --- | --- | --- | --- |
| P0 | 02 공시내역 변환 | 변환된 행을 연도별 목록에 모두 보관하지 않고 임시 SQLite로 직접 스트리밍한다. | 입력 순서, 최초 접수번호 유지, 중복 집계, FTS 생성과 원자적 파일 교체가 달라지지 않아야 한다. |
| P0 | 07 HTML 변환 | 누적 결과 JSON 전체를 반복해서 다시 쓰는 방식을 청크형 checkpoint 또는 내부 저장소 방식으로 바꾼다. | 현재 문서가 전체 중간 payload 저장을 요구하므로 구현 전에 문서와 복구 계약을 먼저 변경해야 한다. |
| P1 | 작업 API | 완료된 대용량 결과는 요약·파일 경로·hash만 메모리에 남기고 세부 결과는 페이지 단위로 읽는다. | API 응답과 화면 조회 방식의 계약 변경이 필요하다. |
| P1 | 01·03 | 변경 불가능한 검사 산출물과 인덱스 검색 구조를 검토한다. | 기존의 독립 검증, 최신성 확인과 실패 조건을 약화하면 안 된다. |

## 검증 상태

- 전체 MarketDesk 및 KIND 관련 테스트: **672 passed, 166 skipped**
- 기존 실패 4건: 변경 전과 동일
- 프런트 정적 테스트 160건과 전체 production build: 통과
- 변경 Python 파일 `py_compile` 및 `git diff --check`: 통과

세부 기능 계약은 `docs/disclosures/01-download`부터 `docs/disclosures/07-html-parse`까지의 단계별 문서를 기준으로 한다.
